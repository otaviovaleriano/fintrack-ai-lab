# Current server configuration (pre-Supabase-migration).
# This documents what server/app.js actually reads today. It will change in
# later migration phases once we decide (Phase 7) whether server/ keeps any
# responsibilities after the client talks to Supabase directly.

MONGO_URI=mongodb://localhost:27017/fintrack
JWT_SECRET=replace-with-a-long-random-value
PORT=5000
